"use strict";

require("./core");

require("./interaction");

({});

describe('PIXI', function () {
    it('should exist as a global object', function () {
        expect(PIXI).to.be.an('object');
    });
});
